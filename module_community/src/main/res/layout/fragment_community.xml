<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:id="@+id/community_host_layout"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:background="@mipmap/bg_title_bar">

        <ImageView
            android:id="@+id/take_picture_iv"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_alignParentEnd="true"
            android:scaleType="centerCrop"
            android:src="@mipmap/icon_take_pic" />

        <android.support.design.widget.TabLayout
            android:id="@+id/community_tab_layout"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_alignParentStart="true"
            android:layout_marginEnd="10dp"
            android:layout_marginStart="20dp"
            android:layout_toStartOf="@+id/take_picture_iv"
            app:tabIndicatorColor="@color/colorWhite"
            app:tabIndicatorFullWidth="false"
            app:tabIndicatorHeight="3dp"
            app:tabMode="fixed"
            app:tabSelectedTextColor="@color/colorWhite"
            app:tabTextAppearance="@style/TabLayoutTextStyle"
            app:tabTextColor="@color/colorWhite" />
    </RelativeLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/community_view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/community_host_layout" />

</RelativeLayout>