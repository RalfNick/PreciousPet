<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        android:id="@+id/pet_adoption_layout"
        layout="@layout/layout_title" />

    <com.yyydjk.library.DropDownMenu
        android:id="@+id/pet_adoption_drop_menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/pet_adoption_layout">

    </com.yyydjk.library.DropDownMenu>

    <com.scwang.smartrefresh.layout.SmartRefreshLayout
        android:id="@+id/pet_adoption_refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/pet_adoption_drop_menu"
        app:srlAccentColor="@android:color/white"
        app:srlPrimaryColor="@color/white">

        <com.scwang.smartrefresh.layout.header.ClassicsHeader
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:srlAccentColor="@color/textGray"
            app:srlDrawableArrow="@mipmap/arrow_refresh"
            app:srlEnableLastTime="false"
            app:srlTextSizeTime="12sp"
            app:srlTextSizeTitle="14sp" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/pet_adoption_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:overScrollMode="never" />

        <com.scwang.smartrefresh.layout.footer.ClassicsFooter
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </com.scwang.smartrefresh.layout.SmartRefreshLayout>

    <FrameLayout
        android:id="@+id/pet_adoption_container_fl"
        android:layout_width="match_parent"
        android:layout_height="49dp"
        android:layout_alignParentBottom="true"
        android:background="#E6FFFFFF">

        <TextView
            android:id="@+id/pet_adoption_title_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:drawableStart="@mipmap/icon_release_adoption"
            android:drawablePadding="6dp"
            android:text="@string/release_adoption"
            android:textColor="#FFFF0000"
            android:textSize="15sp" />

    </FrameLayout>
</RelativeLayout>